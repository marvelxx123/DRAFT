name: Deploy to SiteGround

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare site files
        run: |
          mkdir -p deploy
          cp 904garage.html deploy/index.html
          cp garage-door-repair-*.html deploy/
          cp sitemap.xml robots.txt 404.html adhd.html daily.html deploy/
          cp siteground-upload/.htaccess deploy/
          find deploy/ -name "*.html" -exec sed -i "s|fetch('/api/contact'|fetch('https://draft-production-78c1.up.railway.app/api/contact'|g" {} \;

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          port: 21
          protocol: ftp
          local-dir: ./deploy/
          server-dir: public_html/
          dangerous-clean-slate: false
